html
    head
        title Change password
    body
        if(message)
        h4 #{message}
        form(action = "/user/changePassword" method = "POST")
            input(name = "oldPassword" type = "password" required placeholder = "Password")
            input(name = "newPassword1" type = "password" required placeholder = "Password")
            input(name = "newPassword2" type = "password" required placeholder = "Password")
            button(type = "Submit") Change
        div
        a(href='/user/myprofile') Back to my profile